# 智慧引才图谱 · 解决方案建议

> 场景：高层次人才引进「岗位需求 × 人才简历」智能匹配与定向推荐
> 依据：工作坊产出物预填稿（表一 · 从零起步）+ 业务流程梳理 + 工作量与数据梳理

---

## 一、需求理解（我从预填稿中读到了什么）

### 1. 业务背景与规模
- 深圳供电局「人才飞地」负责年度高层次人才引进。
- 每年接收**全网 300+ 个岗位需求**，通过多渠道收集**高端人才简历 2000+ 份**。
- 简历渠道：猎头直推、进校园、邮箱、交流会。

### 2. 现状流程（7 步，全部靠表格 + 邮件 + 打包文件，无系统）

| 步骤 | 动作 | 责任方 |
|---|---|---|
| 1 | 按既定格式填岗位需求表 | 业务部门 |
| 2 | 单位内部确认后发出 | 用人单位 HR |
| 3 | 汇总需求、收集简历 | 人才飞地 HR |
| 4 | 初审学历/专业/年龄基础条件 | 人才飞地 HR |
| 5 | **全量打包发送所有单位** | 人才飞地 HR |
| 6 | 各单位自己挑选（只看近期，前面漏掉） | 用人单位 HR |
| 7 | 推送业务部门做专业筛选 | 用人单位 HR |

### 3. 核心痛点（预填稿明确列出）
1. **漏看**：全量盲推，各单位没时间看，只看近期推送，早先推送的简历整体被漏掉。
2. **沉睡**：简历发出去后无跟踪，同一批简历被反复推送或彻底沉睡。
3. **无跟踪**：谁被选中、谁已不找工作，完全不知道。
4. **台账查不动**：Excel 台账字段有限，简历内容无法检索。
5. **简历库不完整**：猎头直推渠道简历不经人才飞地，无法纳入。

### 4. 用户明确希望 AI 做的三件事
1. 批量解析各渠道简历、提取关键信息，建成可检索的标准简历库；
2. 按岗位需求自动匹配初筛并定向推送，用人单位可随时检索历史简历；
3. 用标签记录选中/退出状态，保持简历库活性。

### 5. 用户明确划定的边界（红线）
- ❌ 不由 AI 决定最终推荐人选（必须人工复核精选）；
- ❌ 不由 AI 直接对外发送任何推荐结果；
- ❌ 专业筛选仍由业务部门做；
- ❌ 简历数据仅限内部环境处理。

---

## 二、对初步解决方案的评估

预填稿给出的初步方案，**整体方向正确、边界清晰、演示范围收窄得当**，是一份合格的"需求层"方案。但从"能不能落地"的技术层看，存在几个需要补强的关键缺口。

### ✅ 合理之处（值得保留）

1. **痛点抓得准**：聚焦"全量盲推→漏看→沉睡"这个真正的价值断裂点，而非泛泛的"提升效率"。
2. **边界划得清**：明确"AI 只做初筛、人做决策"的人机分工，符合人资合规与风险控制要求。
3. **"活的简历库"理念好**：用标签管理简历状态，抓住了"简历资产随业务持续增值"的本质。
4. **收窄到位**：一个使用者（人才飞地 HR）、一个场景（新岗位→匹配推送）、一条流程，演示主线工作量可控。
5. **完成标准可量化**："20 份简历 5 分钟入库、单岗 3 分钟出 10 人名单、理由可追溯"，当场可验证。

### ⚠️ 关键缺口（需要补强，否则难以真正落地）

1. **匹配逻辑未定义**——只说了"按岗位需求自动匹配"，但没说清楚"怎么匹配"。这是产品成败的核心，必须给出可落地的匹配机制（硬性条件如何过滤、择优条件如何打分、理由如何生成）。

2. **状态流转未定义**——只说了"带标签"，但没定义状态之间的流转规则。如果"已推送"之后没有"被选中/未选中/已退出"的回流路径，简历库依然活不起来。

3. **数据回流机制缺失**——现状最大的痛点是"发出后无跟踪"。方案若只是"推出去"，而各单位不反馈结果，闭环依然断的。需要设计一个**极轻量的反馈机制**，让单位能一键回填结果。

4. **简历解析准确率风险**——简历格式混杂（含扫描件），AI 解析准确率需要兜底机制（人工可修正字段），否则"库里数据是错的"比"没有库"更危险。

5. **猎头渠道简历缺口**——预填稿已识别，但建议作为二期明确规划，而非简单搁置。

---

## 三、我的解决方案

在保留预填稿合理框架的基础上，补齐五个关键缺口，形成一套**可落地、可演示、可迭代**的完整方案。

### 方案总览：人机分工

```
AI 做：解析入库 → 硬性过滤 → 择优排序 → 生成候选+理由 → 状态提醒
人做：岗位条件确认 → 复核精选 → 触发推送 → 记录反馈 → 维护状态
```

### 模块一：简历解析入库（对应"简历库是活的"）

**核心设计：解析 + 人工修正兜底**

1. 批量上传（PDF/Word/图片，含扫描件走 OCR）。
2. AI 抽取结构化字段：
   - **硬性字段**：姓名、学历、专业、年龄、联系方式；
   - **择优字段**：工作履历、研究方向、技能、个人意愿（求职意向/期望城市/是否在职）。
3. **关键补充**：解析结果带**置信度标记**，低置信字段高亮，允许人才飞地 HR 手动修正后确认入库。避免"错数据进库"。
4. 入库即生成"简历名片"，默认状态为"在库"。

### 模块二：岗位需求结构化（对话式）

预填稿提的"对话访谈生成标准清单"方向正确，我补充为**结构化字段输出**，因为匹配引擎需要的是字段而非自由文本：

| 条件类型 | 字段 | 用途 |
|---|---|---|
| 硬性条件 | 学历、专业（大类/方向）、年龄界限 | 第一级规则过滤 |
| 择优条件 | 研究方向、技能栈、履历经验、个人意愿 | 第二级语义打分 |

对话式访谈的目的，是引导业务部门把模糊需求落到这些字段上，输出一张可机读的岗位需求清单。

### 模块三：两级匹配引擎（补缺口 1 —— 本方案的核心）

这是对预填稿最大的补强。匹配分两级，各司其职：

- **第一级 · 硬性过滤（确定性规则）**：学历、专业、年龄用规则引擎精确比对，不满足直接排除。透明可审计、绝不误杀。
- **第二级 · 择优排序（语义打分）**：对通过硬性过滤的简历，用向量检索 + 大模型对研究方向、技能、履历、意愿做语义匹配度打分，输出 Top N 并生成**逐条匹配理由**。

**为什么两级解耦？**
- 硬性条件（学历/专业/年龄）必须 100% 准确，用规则保证；
- 择优条件是非结构化文本，规则写不死，用语义模型保证相关性；
- 每名候选人都附理由，理由可点开对应简历原文，**可解释、可追溯**。

### 模块四：简历状态流转（补缺口 2 —— 让"活"字落地）

定义完整的状态机（见状态流转图）：

| 状态 | 含义 | 谁触发 |
|---|---|---|
| 在库 | 新解析入库，可参与匹配 | 系统自动 |
| 已推送 | 已定向推送给某单位 | HR 触发推送 |
| 被选中 | 单位确认有意向 | 单位反馈 |
| 未选中 | 未被选中，回退在库可再匹配他岗 | 单位反馈 |
| 已退出 | 已不找工作，不再参与匹配 | HR/本人 |

**核心价值**：未选中→回退在库→可再匹配其他岗位，让每份简历都能持续流转，而不是一次性消费。

### 模块五：轻量反馈回流（补缺口 3 —— 直击"无跟踪"痛点）

这是方案里最容易被忽略、却最关键的一环。现状是"发出后无跟踪"，如果不解决反馈，闭环依然断。

**设计一个极轻量的反馈机制**：
- 推送时，附一个**反馈链接/一键回填**（每条候选人有"有意向 / 无意向"按钮）；
- 单位 HR 点一下即可，无需登录复杂系统；
- 反馈结果自动回流，更新简历状态（被选中/未选中）和岗位匹配记录。

> 原则：让"记录反馈"这件事比"不记录"更省事，闭环才会真正转起来。

### 模块六：外部寻源建议（加分项）

库内无匹配时，给出外部寻源方向（对口高校院系、科研院所、相关企业）。这是预填稿的亮点，保留为**加分项**，不进入演示主线。

### 分阶段落地建议

| 阶段 | 范围 | 目标 |
|---|---|---|
| **一期（演示主线）** | 模块一 + 二 + 三 + 四（基础状态）+ 名单导出 | 跑通"上传→解析→匹配→推送→导出" |
| **二期** | 模块五（反馈回流）+ 状态机完整流转 | 补上"无跟踪"闭环 |
| **三期** | 模块六（外部寻源）+ 猎头渠道简历接入 | 简历库完整性 + 增值能力 |

> 建议：一期演示主线中，就把"状态标签"和"匹配理由可追溯"做进去（预填稿的完成标准已含），因为它们是最能体现产品亮点的部分；反馈回流可作为二期，但在汇报里明确讲清"这是补无跟踪闭环的关键一步"。

### 与预填稿完成标准的对应关系

预填稿产出物 10 的完成标准，我的方案完全覆盖且更具体：

| 预填稿标准 | 本方案落点 |
|---|---|
| 20 份简历 5 分钟入库带标签 | 模块一（含置信度修正）+ 模块四 |
| 单岗 3 分钟出 10 人候选+理由可追溯 | 模块三（两级匹配 + 逐条理由） |
| 精选后导出 Excel、标签更新为"已推送" | 模块四（状态流转） |
| （未明说，但根源痛点）发出后无跟踪 | 模块五（反馈回流）二期补上 |

---

## 四、结论

1. **初步方案方向正确**，痛点判断准、边界划得清、演示范围收窄得当，可直接作为需求基线。
2. **需补强五个技术缺口**，其中"匹配逻辑""状态流转""反馈回流"三项是决定产品能否真正落地和体现价值的关键。
3. **推荐路线**：一期做"解析→匹配→推送→导出"演示主线（含状态标签 + 可追溯理由），二期补"反馈回流"闭环，三期做外部寻源与猎头渠道接入。

一句话总结：**预填稿把"做什么"想清楚了，我的方案把"怎么做才能真落地"补上了。**
