# 智慧引才图谱 · 技术栈选型说明（终版）

> 用途：供开发团队直接参照搭建项目
> 前提约束：简历含个人敏感信息，**只允许内网处理** → 所有 AI 能力必须内网处理，禁止调用公网云 API

**已确认的三个前提（2026-08-19）**：
1. ✅ 内网有 GPU；
2. ✅ 有自有内部大模型平台 → **大模型不自部署，直接调内部平台内网 API**；
3. ✅ 扫描件占比未知，暂不考虑处理速度，**以稳定性为主**。

---

## 一、选型总原则

1. **AI 全内网化**：大模型走内部平台内网 API，向量模型、OCR 本地内网部署，全程不出内网。
2. **大模型不自建**：既然有内部平台，直接对接其接口，省去显卡规划、模型量化、推理服务运维，最省力也最稳定。
3. **单体优先，够用即可**：一期演示主线是"上传→解析→匹配→推送→导出"，用单体应用 + 异步任务即可，不上微服务、不上 K8s。
4. **Python 主导 AI、React 主导前端**：简历解析/匹配是 AI 密集型，选 Python 生态最顺；前端后台选 React + Ant Design 最省力。
5. **稳定性优先于速度**：解析/匹配走异步队列 + 重试机制，宁可慢一点，不丢单、不崩。

---

## 二、分层技术栈（明确清单）

### 1. 前端

| 项 | 选型 | 版本 | 说明 |
|---|---|---|---|
| 框架 | React | 18.x | 生态最成熟 |
| 语言 | TypeScript | 5.x | 类型安全 |
| 构建 | Vite | 5.x | 秒级冷启动，开发体验好 |
| UI 组件 | Ant Design | 5.x | 中文后台组件最全（表格/上传/表单/标签/弹窗） |
| HTTP | Axios | 1.x | 请求封装 |
| 状态 | Zustand | 4.x | 轻量，够用 |
| 路由 | React Router | 6.x | 页面导航 |

> 前端核心页面仅 4 个：①简历上传解析 ②岗位需求录入（对话式）③匹配结果与理由 ④简历库检索+标签管理。Ant Design 的 Table/Upload/Form/Tag/Modal 直接覆盖。

### 2. 后端

| 项 | 选型 | 版本 | 说明 |
|---|---|---|---|
| 语言 | Python | 3.12 | AI 生态最全 |
| Web 框架 | FastAPI | 0.115+ | 异步高性能、自动生成 API 文档、类型友好 |
| 异步任务 | Celery | 5.x | 批量解析简历是耗时任务，必须异步 |
| 消息队列 | Redis | 7.x | 兼做 Celery broker + 缓存 |
| ORM | SQLAlchemy | 2.0 | 数据模型与迁移 |
| 迁移 | Alembic | 1.x | 数据库版本管理 |
| 表格导出 | openpyxl | 3.x | 生成 Excel 名单 |
| 依赖管理 | uv 或 pip + poetry | — | 建议 uv，快且锁版本 |

### 3. AI 能力层（内网）

| 能力 | 选型 | 说明 |
|---|---|---|
| 大模型（LLM） | **自有内部大模型平台（内网 API）** | 简历结构化抽取、岗位需求结构化、匹配理由生成。**不自部署**，直接调内部平台接口 |
| 向量模型（Embedding） | **BGE-M3**（本地 GPU 部署） | 中文语义检索效果最好、开源；**需确认内部平台是否也提供 Embedding 接口，若提供则更省事** |
| OCR | **PaddleOCR** | 扫描件简历识别，中文准确率高、免费、稳定 |
| 文档解析 | PyMuPDF（PDF）+ python-docx（Word） | 提取简历文本；图片走 OCR |

> **对接内部平台前需确认 4 件事**（直接决定开发方式）：
> 1. 接口协议——是否兼容 OpenAI 格式（`/v1/chat/completions`）？还是自研协议？
> 2. 是否支持**结构化输出 / JSON 模式 / function calling**？简历抽取强依赖这个能力，若支持，抽取质量与稳定性大幅提升。
> 3. 是否提供 **Embedding 接口**？提供则向量层也走平台；不提供则本地用 BGE-M3（GPU 已确认可用）。
> 4. 并发限制与超时——决定批量解析时的队列并发数。
>
> **稳定性兜底设计**（针对"以稳定性为主"）：LLM 调用失败自动重试 3 次 + 指数退避；抽取结果带置信度，低置信字段人工修正后再入库。

### 4. 数据层

| 项 | 选型 | 版本 | 说明 |
|---|---|---|---|
| 数据库 | PostgreSQL | 16 | 结构化数据 + 向量同库 |
| 向量扩展 | pgvector | 0.7+ | 存简历/岗位 Embedding，做语义相似度检索 |
| 文件存储 | MinIO（或本地磁盘） | — | 存简历原件 PDF/Word/图片 |

**核心数据表建议**：

```
resumes        —— 简历主表（id, name, 结构化字段JSON, embedding, status, created_at）
job_requests   —— 岗位需求表（id, 硬性条件JSON, 择优条件JSON, embedding, 状态）
match_records  —— 匹配记录表（job_id, resume_id, score, 理由, 推送状态）
```

---

## 三、匹配引擎的技术实现（对应方案的两级匹配）

| 层级 | 技术手段 | 实现要点 |
|---|---|---|
| 第一级硬性过滤 | SQL 精确条件查询 | 学历/专业/年龄走数据库 WHERE 过滤，`resumes` 表存结构化字段，秒级完成 |
| 第二级择优排序 | pgvector 余弦相似度 + LLM 重排 | ① 岗位 embedding 与简历 embedding 算余弦相似度，取 Top 30~50；② 用 LLM 对 Top 名单逐条生成"匹配理由"并二次精排 |

**为什么先向量粗排、再 LLM 精排**：2000+ 简历若全部交给 LLM 逐条打分，太慢太贵；先用向量相似度收窄到几十份，再让 LLM 精排 + 生成理由，兼顾速度与质量。

---

## 四、部署架构（内网）

```
浏览器 ──▶ Nginx ──▶ FastAPI 应用
                        │
                        ├──▶ PostgreSQL（pgvector）
                        ├──▶ Redis（队列）
                        ├──▶ Celery Worker（解析任务）
                        ├──▶ 内部大模型平台（内网 API，OpenAI 兼容或自研协议）
                        ├──▶ BGE-M3（本地 Embedding 服务，若平台不提供）
                        └──▶ MinIO（简历原件）
```

- 全部用 **Docker Compose** 编排，一条 `docker compose up -d` 拉起整套环境。
- 内网隔离：不暴露公网端口，仅内部 IP 访问；后续可加企业 SSO 登录。

---

## 五、MVP 演示主线的技术裁剪（先跑通，再扩展）

一期只做演示主线，以下能力可**暂时裁掉**，避免过早复杂化：

| 暂缓项 | 理由 |
|---|---|
| Celery + Redis 异步 | 20 份简历解析，用 FastAPI 后台任务（BackgroundTasks）即可，无需队列 |
| MinIO | 先存本地磁盘目录 |
| 反馈回流（模块五） | 二期再做 |
| 多用户登录/权限 | 一期单用户，内部工具 |
| 外部寻源 | 三期 |

**MVP 最小技术集**：React + Ant Design ｜ FastAPI + SQLAlchemy ｜ PostgreSQL + pgvector ｜ 内部大模型平台 API ｜ BGE-M3 ｜ PaddleOCR ｜ PyMuPDF/python-docx。这套即可满足预填稿的完成标准（20 份简历 5 分钟入库、单岗 3 分钟出 10 人名单、理由可追溯、导出 Excel）。

---

## 六、推荐的开发里程碑

| 里程碑 | 内容 | 验证标准 |
|---|---|---|
| M1 环境搭建 | Docker Compose 拉起 PG/pgvector、Embedding 服务、后端、前端骨架；联调内部平台 API | 本地能访问空页面与 API 文档 |
| M2 简历解析入库 | 上传 PDF/Word/图片 → 解析 → 结构化字段入库（含置信度修正） | 20 份简历 5 分钟内入库 |
| M3 岗位需求结构化 | 对话/表单生成硬性+择优条件清单 | 一个真实岗位输出标准字段 |
| M4 两级匹配 | 硬性过滤 + 向量粗排 + LLM 精排生成理由 | 单岗 3 分钟出 10 人候选 |
| M5 标签与导出 | 状态流转 + 导出 Excel 名单 | 精选后导出、标签更新为"已推送" |

---

## 7 个前置问题（已确认 3 个，剩 4 个待与内部平台对接）

1. ~~内网硬件~~ ✅ 有 GPU；
2. ~~大模型来源~~ ✅ 有内部平台，接其内网 API；
3. ~~速度要求~~ ✅ 以稳定性为主，走异步队列 + 重试。

**待与内部平台确认的 4 件事**：

| # | 需确认 | 影响 |
|---|---|---|
| 1 | 接口协议是否 OpenAI 兼容 | 决定后端用 openai SDK 还是自写客户端 |
| 2 | 是否支持结构化输出/JSON 模式/function calling | 决定简历抽取的稳定性与质量 |
| 3 | 是否提供 Embedding 接口 | 提供则省掉本地 BGE-M3 |
| 4 | 并发限制与超时参数 | 决定批量解析的队列并发数 |
